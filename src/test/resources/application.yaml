spring:
  cloud:
    aws:
      ssm:
        packages:
          - aaa.aaa.aaa
          - bbb.bbb.bbb

---
spring:
  config:
    activate:
      on-profile: local
  cloud:
    aws:
      ssm:
        profile: opsdev-sts

